import Styled from "react-emotion";

const Style = Styled('div')`
  .ui.container {
    position: absolute; 
    justify-content: center; 
    display: flex;
    width: 100%;
  }

  .ui.message {
    width: 50%;
  }
`

export default Style;